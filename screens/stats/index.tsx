import { SafeAreaView, Text } from 'react-native';

const StatsScreen = () => {
   return (
      <SafeAreaView>
         <Text>Hello World</Text>
      </SafeAreaView>
   );
};

export default StatsScreen;
