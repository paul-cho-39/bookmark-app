import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   container: {
      marginTop: 20,
      marginHorizontal: 5,
   },
   mainText: {
      marginBottom: 10,
   },
   subText: {
      marginBottom: 10,
   },
   inputOutline: {
      borderRadius: 15,
      elevation: 0,
      zIndex: 0,
   },
});

export default styles;
